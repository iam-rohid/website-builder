import { FlexComponent } from "../../../types";

const FlexComponentProps = (props: { component: FlexComponent }) => {
  const { component } = props;
  return <div>Flex Component</div>;
};

export default FlexComponentProps;
