import { TextComponent } from "../../../types";

const TextComponentProps = (props: { component: TextComponent }) => {
  const { component } = props;

  return <div>Text Component</div>;
};

export default TextComponentProps;
